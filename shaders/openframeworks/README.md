# OpenFrameworks

This folder is reserved for OpenFrameworks examples that load the GLSL shader and pass species preset values as uniforms.

Planned example:

- Load a preset JSON file.
- Pass wood parameters to `shaders/glsl/wood.glsl`.
- Render an interactive plane with live parameter sliders.
